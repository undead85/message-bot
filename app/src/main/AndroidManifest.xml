<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      NOTA sobre permisos:
      - El acceso a notificaciones NO se pide con <uses-permission>. Se concede
        manualmente por el usuario en Ajustes > Acceso a notificaciones.
        BIND_NOTIFICATION_LISTENER_SERVICE se declara en el <service> para que
        SOLO el sistema pueda enlazarse a él.
      - REQUEST_IGNORE_BATTERY_OPTIMIZATIONS es opcional: ayuda a que el sistema
        no mate el proceso agresivamente. Google Play lo restringe; para uso
        personal (sideload) no hay problema.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppBot">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LogActivity"
            android:exported="false"
            android:label="@string/log_title" />

        <!-- Re-enlaza el listener automáticamente tras actualizar la app. -->
        <receiver
            android:name=".service.PackageReplacedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
          Servicio de escucha de notificaciones.
          - android:permission asegura que solo el sistema (que posee ese permiso)
            pueda enlazarse al servicio.
          - El intent-filter es obligatorio para que aparezca en
            Ajustes > Notificaciones > Acceso a notificaciones.
        -->
        <service
            android:name=".service.WhatsAppNotificationListener"
            android:exported="false"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>
</manifest>
